<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.testproject.pokemonapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.testproject.pokemonapp.ui.BaseApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pokemon_ball"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pokemon_ball"
        android:supportsRtl="true"
        android:theme="@style/Theme.PokemonApp"
        tools:targetApi="31">
        <activity
            android:name="com.testproject.pokemonapp.ui.splash.SplashActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.HomeActivity"/>
    </application>

</manifest>